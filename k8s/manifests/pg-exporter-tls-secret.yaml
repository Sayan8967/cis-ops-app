apiVersion: v1
kind: Secret
metadata:
  name: pg-exporter-tls
  namespace: cis-ops
type: kubernetes.io/tls
data:
  # These are placeholders. Replace with base64-encoded cert/key before applying.
  tls.crt: "REPLACE_WITH_BASE64_CERT"
  tls.key: "REPLACE_WITH_BASE64_KEY"


