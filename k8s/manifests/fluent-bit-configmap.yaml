apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  namespace: cis-ops
data:
  fluent-bit.conf: |-
    # (config omitted in CI-generated file; use repo version)
  parsers.conf: |-
    # (parsers omitted)
